/*
 * AddEntity Messages
 *
 * This contains all the text for the AddEntity container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddEntity';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddEntity container!',
  },
});

/*
 * ViewRule Messages
 *
 * This contains all the text for the ViewRule container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ViewRule';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ViewRule container!',
  },
});

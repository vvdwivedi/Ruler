/*
 * EntityDashboard Messages
 *
 * This contains all the text for the EntityDashboard container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EntityDashboard';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EntityDashboard container!',
  },
});

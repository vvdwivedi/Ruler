/*
 * ViewEntity Messages
 *
 * This contains all the text for the ViewEntity container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ViewEntity';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ViewEntity container!',
  },
});

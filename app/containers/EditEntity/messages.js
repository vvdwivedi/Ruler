/*
 * EditEntity Messages
 *
 * This contains all the text for the EditEntity container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EditEntity';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EditEntity container!',
  },
});

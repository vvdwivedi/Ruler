/*
 * SelectBox Messages
 *
 * This contains all the text for the SelectBox component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SelectBox';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SelectBox component!',
  },
});

/*
 * ExpressionBox Messages
 *
 * This contains all the text for the ExpressionBox component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ExpressionBox';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ExpressionBox component!',
  },
});

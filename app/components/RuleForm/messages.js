/*
 * RuleForm Messages
 *
 * This contains all the text for the RuleForm component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.RuleForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RuleForm component!',
  },
});
